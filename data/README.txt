# Директория данных (закрыта от веба через .htaccess).
# Здесь создаются:
#   tracker.sqlite     — база данных
#   password_hash.txt  — bcrypt-хеш пароля
#   auth_secret.txt    — секрет подписи auth-cookie
